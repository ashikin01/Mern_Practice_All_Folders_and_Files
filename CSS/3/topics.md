#Layout

1. Flow Mode : normal mode, default css
2. Postion Mode (position<value>)
    - position attribute to enter layout position, by default everything is static. 

    a. static->indicates to flow mode
    b.relative
    c.absolute
    d.fixed
    e.sticky
    f. supportive properties
        1-top,left,bottom,right
        2-z-index
3. Flex Box mode
    1-Parent
    2-Child
4. Grid Mode

-static
-relative
- fixed
-sticky
supportive properties
-top left bottom right
z-index